# GPT-6 Astra 接入工作流 · 定位建议报告

> 日期：2026-09-07
> 依据：模型自述中标注【官方文档】的条目 + 本项目 CLAUDE.md 五阶段流程 + 项目实际踩过的坑。自述中标"不知道"的能力一律按未验证处理，不作为定位依据。

---

## 一、结论

**主岗位：规则型验收（reviewer agent 的业务审核 + 合规审核）。**
**副岗位：机械层执行与批处理（跨脚本一致性抽取、路径与状态维护、无限画布搭建、大批量结构化校验）。**
**不放：编剧 / 音乐总监 / 最终审美验收 / 制片人总调度 / 目标制定。**

---

## 二、能力对照（只列有官方出处的）

| 能力 | 官方数据 | 对本项目意味着什么 |
|---|---|---|
| 上下文 | 1,050,000 tokens | 10 个脚本 + 全部 outputs + assets 可一次装下，跨集一致性检查不用分批 |
| 单次输出 | 128,000 tokens | 足够输出整份审核报告 |
| 推理档位 | low / medium / high / xhigh / max | 校验类任务用 low/medium 控成本，冲突裁定用 high |
| 文件输入 | PDF / .docx / .md，单文件 50 MB | 可直接读 `番外-9.3.docx` 一类原稿 |
| 音频 / 视频输入 | 不支持 | 无法承担音乐总监、无法验收成片 |
| 视频生成 | 不支持 | 与 Seedance 无重叠，只能写提示词不能出片 |
| 结构化输出 | json_schema + strict 强制格式 | 审核结论可机读，但官方明说格式对不保证内容真 |
| 本地执行 | Codex on Windows，PowerShell + 沙箱 | 能直接在本仓库改文件、跑脚本 |
| 多智能体 | Responses API 多 agent 编排，beta，默认 3 并发 | 可做子编排，但 beta 且并发低 |
| 中文创作 / 方言 | 无官方评测 | ep02-04 四川话剧本不能交给它定稿 |
| 长周期任务成功率 | 无官方数据 | 不能把它当"放手跑一天"的执行者 |
| 幻觉率 | 无官方数据，官方提示引用可能编造 | 每条审核结论必须带文件+行号，程序回查 |
| 价格 | 输入 $10 / 输出 $50 每百万 token；输入超 272K 时输入 2 倍、输出 1.5 倍；Batch/Flex 半价 | 适合"大输入、小输出"的审核，不适合"大输出"的剧本生成；单次投喂控制在 272K 以内 |
| 中国大陆 | 不在官方支持地区列表 | 合规风险，见第五节 |

---

## 三、为什么放验收，而不是它自荐的"规划 + 调度"

### 3.1 放验收的三条依据

**依据 1：这正是本项目一直在出血的地方，而且出血的全是规则型错误。**
- 脚本06 的视频提示词 Cut 时长违规，靠手写 `temp_fix_cuts.py` 补救。
- 番外01 导演稿把 P02 写成"三组微切 / 硬切 / 故事板"，与 `.project-config.json` 的 single-shot 和 Seedance 每条 ≥4 秒的硬约束冲突，业务审核 FAIL。
- 脚本08 导演分析整个换了故事，与剧本终稿不符，还引入了剧本里没有的狐狸、兔子两个角色，反馈池里记了两条。

这三类都是"拿规则一比就能发现"的错误，正是自述里它认定的强项：把明确规则转成检查表、字段比对、跨文件一致性表。它的弱项（审美、方言地道度）在验收这一步也不是主要工作量。

**依据 2：创作与审核分开用两个模型家族，独立性比同家族更强。**
CLAUDE.md v12.0 把 reviewer 从 director 独立出来，理由是"每次空白上下文开始，更客观"。目前创作和审核都是 Claude，同一模型的盲点会同时出现在写和审两边。换成 Astra 审 Claude 写的东西，是真正意义上的交叉验证。

**依据 3：成本结构匹配。**
审核是大输入、小输出。它输出价 $50 是输入价的 5 倍，把它放在输出量最小的岗位最划算。同样的钱放到编剧岗，产出的是它没有官方评测的中文创意文本，性价比反过来。

### 3.2 不放总调度的原因

本项目的调度者（制片人）已经是 Claude Code 主循环，它管介入模式、状态路由、Resumable subagent、反馈池。再加一个调度者就是双头，会互相覆盖 `.agent-state.json` 和 `00_当前状态.md`。它的多智能体编排目前是 beta、默认只 3 并发，也不如现有机制成熟。它自荐"规划 + 调度"是基于"官方支持编排"这个泛化能力，不是基于我们这个已经有调度者的具体项目。

### 3.3 不放编剧、音乐、审美验收的原因

- 编剧：中文创作和四川话没有官方评测，它自己也说"不能承诺方言自然"。四川话 60s 短剧是我们的一条产品线，不能拿没验证过的能力赌。
- 音乐总监：不支持音频输入，连自己写的 Suno 提示词生成的结果都听不了。
- 审美验收：它自己列为最不适合。这一层留给导演 agent 复盘和人。

---

## 四、落地方案

### 4.1 主岗位：接管 reviewer 的规则层

1. 通过 Codex 或 Responses API 调用 `gpt-6-astra`，投喂四个审核 skill、compliance-review-skill、`_shared/ai-generation-constraints.md`、`.project-config.json`，加上待审文件和它的上游依据文件。
2. 输出用 strict JSON Schema 固定：每条问题必须含 `file`、`line`、`quote`（原文短引）、`rule_source`（违反了哪条规则、在哪个 skill 的哪一行）、`severity`。
3. 接收前用脚本回查：`quote` 是否真的存在于 `file` 的 `line` 附近。查不到的条目丢弃并计数。这一步是针对它自己承认的"可能编造引用"的硬防线。
4. 审美类、气质类判断仍由 Claude director 做复盘，写入 `00-quality-report.md`。Astra 只签规则层。

### 4.2 副岗位：机械层批处理

- 跨脚本一致性报告的重生成：读全部 10 个脚本 + 角色档案，输出冲突表。1M 上下文让它一次装下，不像现在要分批。
- 无限画布搭建：`00_无限画布适配总览.md` 本来就是写给 Codex Agent 的，直接兑现。
- 全项目路径引用重写、速查表重生成、反馈池状态同步这类机械维护。
- 推理档位用 low 或 medium，开 Batch 拿半价。

### 4.3 保持不动

- 制片人调度、bianju、director 讲戏、art-designer、music-director、storyboard-artist 的创作产出，继续由现有 Claude agent 负责。
- Astra 不写 `00_当前状态.md` 和 `.agent-state.json`，只读。

---

## 五、风险与边界

1. **合规**：官方页面明确中国大陆不在 API 支持地区，从未列地区访问可能导致封号。这个风险由你自己评估，本报告只负责把它写清楚。你现有 Codex 会话是什么授权路径未知，如果它已经在正规授权下运行，本方案沿用同一路径。
2. **引用幻觉**：无官方幻觉率数据。4.1 第 3 步的程序回查不能省。
3. **272K 成本悬崖**：单次输入一旦超过 272K，全请求输入价翻倍。跨脚本检查时按"角色档案 + 10 个剧本"分组投喂，不要一次塞进全部 outputs。
4. **长周期可靠性无数据**：不要让它无人值守跑整条流水线。每次调用给它一个明确的单一任务和验收标准。

---

## 六、预期提升

- 规则型错误在审核阶段拦住，而不是像脚本06、脚本08、番外01 那样流到下游再返工。
- 创作和审核分属两个模型家族，同类盲点不再同时出现在两端。
- 跨集一致性检查从分批人工拼接变成一次性全量比对。
- 审核成本落在它最便宜的用法上：大输入缓存、小输出、低推理档、Batch 半价。
