# 📖 凯凯项目 · 用户操作手册 v7.0

> 这份手册是给**用户（你）**看的；`CLAUDE.md` 是给 AI 团队看的。
> 不需要从头读完，按需查阅即可。

---

## 1. 这是什么

一个由 5 个 AI agent 组成的影视制作团队（编剧/导演/服化道/音乐总监/分镜师），从**选题或剧本**出发，自动协作产出三套**工业级可直接使用**的提示词：

- 🎬 **分镜提示词** → 喂给 Seedance / 可灵 / Runway 生成视频
- 🎨 **视觉提示词** → 喂给 Image2（首选，文字+画面细节控制更精确）/ Nano Banana Pro / 即梦 生成参考图
- 🎵 **音乐提示词** → 喂给 Suno 生成音乐

你只需要：放素材 → 输入指令 → 拿提示词 → 自己去 AI 工具生成 → 剪辑成片。

🎯 **v7.0 核心能力**：
- 内置 7 大短视频平台规律对照（抖音/快手/小红书/视频号/B站/YouTube/TikTok）+ 受众参与度审视
- **对标爆款机制**（v6 新增）：你可以给爆款作品作为气质参照系；没有也可以让 KAIKAI 推荐 3-5 个；也可以明确声明不要对标
- **反向反馈环**（v6 新增）：下游 agent 发现上游问题时正式上报；你也能通过 `~feedback` 主动注入意见到流水线
- **万能故事板双轨模式**（v7.0 新增）：分镜师能根据剧情自动选「故事板模式」（一张 PNG 全部拍完）或「单镜模式」（传统多参考图拼接）；故事板风格还能从 5 种中（写实电影感 / 迪士尼 3D / 史诗写实 / 商业干净 / 黑白漫画线稿）智能选择
- 所有产出都为"在目标平台被看到"服务，不只是"获奖短片"

---

## 🎴 4 张备忘卡（不记得指令就看这个）

### 🟢 卡 1：路径 A 启动（从选题开始）

```
1. 把灵感/梗概放到  input/选题/
2. 输入  ~write
3. 编剧会采访你 → 你回答 → 团队接力 → 拿交付清单
```

### 🟡 卡 2：路径 B 启动（已有剧本）

```
1. 把剧本文件放到  input/剧本/  （建议命名 ep01-剧本名.md）
2. 输入  ~start
3. 制片人会先问你 3 个问题（约 2-3 分钟回答）：
     ① 目标平台（抖音/小红书/B站 等）
     ② 目标受众画像（写给一个具体的人，不是"年轻人"）
     ③ 对标爆款（v6 新增；有就给，没有让我推荐，不要也行）
4. 之后自动进入导演 → 服化道 → 音乐 → 分镜
```

### 🆘 卡 3：忘了下一步该做什么

| 你的状态 | 输入这个 |
|---------|---------|
| 不知道项目做到哪了 | `~status` |
| 想看所有指令 | `~help` |
| 想改目标平台/受众画像/**对标爆款** | `~settings` |
| 想给上一阶段提修改意见 | `~feedback <你的意见>`（v6 新增） |
| 想换协作节奏 | `~mode` |
| **完全懵了** | 直接用日常话问："我下一步该做啥？" 制片人会自动救援 |

### 🚀 卡 4：阶段速查（按顺序走）

```
~write              → 编剧创作（路径 A 用）
~start [epXX]       → 导演分析（含 v7.0 「建议形态」字段输出）
~design [epXX]      → 服化道设计
~music [epXX]       → 音乐设计
~skip music [epXX]  → 跳过本集音乐（ASMR/纯环境音项目）
~prompt [epXX]      → 分镜编写（v7.0 双轨模式，拿到最终交付清单 + episode-master）
```

集数参数可选（如果只有一集）。每个阶段结束系统会主动告诉你下一步。

**双轨控制（v7.0 新增，可选）**：
```
~storyboard-mode storyboard ep01    → ep01 锁故事板模式
~storyboard-mode single-shot ep02   → ep02 锁单镜模式
~storyboard-style cinematic_realism → 锁故事板风格为「写实电影感」
```
不调用 = 交给 AI 逐剧情点决策树自选（推荐默认行为）。

---

## 2. 三分钟快速开始

### 路径 A：从选题创作（你只有想法）

```
1. 把灵感/梗概/参考素材放到  input/选题/
2. 在 Claude Code 里输入：~write
3. 跟着编剧采访回答问题
4. 编剧产出 → 你确认 → 自动进入导演 → 服化道 → 音乐 → 分镜
5. 拿到交付清单，去对应 AI 工具生成
```

### 路径 B：从剧本开始（你已经有剧本/歌词）

```
1. 把剧本文件放到  input/剧本/  （建议命名 ep01-剧本名.md）
2. 在 Claude Code 里输入：~start
3. 直接进入导演分析 → 服化道 → 音乐 → 分镜
4. 拿到交付清单，去对应 AI 工具生成
```

---

## 3. 项目类型对照表

| 项目类型 | 路径 | 介入模式 | 音乐策略 | 备注 |
|---|---|---|---|---|
| **短片**（3-5 分钟） | A 或 B | key_node（默认）| 极简型（1 首主题曲）| 大部分情况 |
| **短剧**（多集） | A | key_node | 标准型 | 编剧采访时选"短剧模式" |
| **MV / 音乐驱动** | B（先放歌词）| key_node | 主题曲先做 | 把歌词放 input/剧本/ |
| **ASMR / 纯环境音** | A 或 B | autonomous | `~skip music` | 全程不要音乐 |
| **改编（小说→片段）**| B | full_control | 极简型 | 重要项目精雕细琢 |
| **批量出片** | A | autonomous | 标准型 | 让团队全自动跑 |

---

## 4. 提示词怎么用（最重要的一节）

### 拿到交付清单后的标准工作流

> v7.0 双轨产物说明：
> - **单镜模式**项目：与 v6.1 一致，走传统多参考图拼接路线
> - **故事板模式**项目：多一步「生故事板大图」作为中间参考图，另一条路线走

```
第 0 步：读 episode-master.md（v7.0 新增，不要跳）
   outputs/<集数>/episode-master.md
   ↓
   了解本集双轨模式选了哪个、有多少 P0X、所需资产清单、衔接表

第 1 步：生成参考图（先做这个，因为后面要用）
   assets/visual/character-prompts.md  →  **Image2**（v7.0.2 首推） / Nano Banana Pro / 即梦
   assets/visual/scene-prompts.md      →  **Image2** / Nano Banana Pro / 即梦
   ↓
   保存所有参考图到本地

   【工具选择提示】（v7.0.2新增）：
   - **Image2**：文字和画面细节控制更精确（推荐首选）
   - **Nano Banana Pro**：多图参考+人物一致性能力优异
   - **即梦**：中文语义理解好、充值索费低
   - 提示词本身是通用的，三个工具都可以复粘同一份提示词

第 1.5 步：生故事板大图（仅故事板模式项目需要）
   outputs/<集数>/storyboards/P01-storyboard-prompt.md → **Image2**（v7.0.2 首推） / Nano Banana Pro / 即梦
   outputs/<集数>/storyboards/P02-storyboard-prompt.md → **Image2** / Nano Banana Pro / 即梦
   ...
   ↓
   保存为 P01-storyboard.png / P02-storyboard.png 等 16:9 横版复合图

第 2 步：生成视频片段
   outputs/<集数>/video-prompts.md  →  Seedance / 可灵 / Runway
   （旧项目仍读 outputs/<集数>/02-seedance-prompts.md）
   ↓
   - 故事板模式：每 Cut 上传唯一参考 → P0X-storyboard.png
   - 单镜模式：每 Cut 上传多参考 → 人物图 + 场景图 + 道具图（按提示词 @图片1/@图片2... 编号顺序）

第 3 步：生成/获取音乐（可与第 2 步并行；音乐采用「双文档」体系）

   📄 原创音乐（少而精，喂给 Suno 自己生成）
   assets/music/music-design.md  →  Suno
   - 复制 ━━━ 复制这里到 Suno ━━━ 之间的内容粘贴
   - 人声歌曲含 [Verse]/[Chorus] 等 meta-tag
   - 纯配乐含 [Instrumental] 标记
   - 对应歌词在 assets/music/lyrics-drafts.md（三版风格的终选稿）

   📄 市面已有音乐（多而精，去音乐平台直接搜）
   assets/music/music-recommendations.md  →  网易云 / QQ 音乐 / Spotify / YouTube Music 等
   - 每条推荐含：多平台搜索名 + 服务于本项目的理由 + 推荐使用片段（时间码）+ 版权提示
   - 适用场景：场景气氛、转场配乐、片尾曲——这些位置用市面成品比 Suno 原创更省力且效果稳定
   - 商用项目：版权提示中标注"商用需采购"的曲目，先去版权方/CC 授权目录确认再用

   选择原则：主题曲/记忆点 OST 用原创（让 Suno 写专属歌）；氛围/场景配乐用市面推荐（直接搜现成的）。

第 4 步：剪辑
   导出所有视频片段、音乐
   按 outputs/<集数>/01-director-analysis.md 的剧情顺序剪
   按 outputs/<集数>/video-prompts.md 的镜头时长建议参考节奏（v7.0+；旧项目读 02-seedance-prompts.md）
   按 outputs/<集数>/episode-master.md §六衔接表 检查跨段过渡（v7.0+）
   音乐分轨：原创主题曲 + 市面氛围曲分轨混音，便于单独调音量
```

### 关键技巧

- **视频时长不必精确对齐音乐**：视频以 15 秒为单位生成，音乐让 Suno 完整生成，剪辑时 trim/loop 即可
- **参考图很重要**：分镜提示词里的 `@图片1` 等引用必须有真实参考图，否则 Seedance 生成质量差
- **多次生成取最佳**：Suno/Seedance 都建议同一提示词生成 3-5 次选最佳
- **歌词三版要选一**：音乐总监会停下等你选 A/B/C，回复字母即可
- **视频生成时不要在提示词里加配乐描述**：视频 AI 不会输出配乐，写了会和后期 Suno/市面音乐双轨冲突——配乐统一在剪辑期叠加

---

## 5. 介入模式怎么选

启动时会问你 1/2/3，按以下场景选：

| 模式 | 适合 | 行为 |
|---|---|---|
| **1. 全自动** | 不重要的项目、批量产出、你很忙 | 全程不停，仅在歌词三版时停一下让你选 |
| **2. 关键节点**（默认）| 大部分项目 | 每个大阶段完成后等你确认才进入下一阶段 |
| **3. 全程介入** | 重要作品、需要精雕细琢 | 子步骤都停下问你 |

中途想改？输入 `~mode`（下一阶段生效）或 `~mode 立即`（立刻重启当前阶段）。

---

## 6. 常用指令速查

```
启动      ~write / ~start
进度      ~status
帮助      ~help
设定      ~settings           （含目标平台/受众/对标爆款）
反馈      ~feedback <文本>    （v6 新增；给上游 agent 提改进意见）
模式      ~mode / ~mode 立即
跳音乐    ~skip music ep01
重做某集  删除 outputs/<集数>/ 下文件后重新 ~start <集数>

# v7.0 新增双轨控制（仅分镜阶段使用，可选）
双轨      ~storyboard-mode <storyboard|single-shot> [<集数|global>]
风格      ~storyboard-style <名>      （仅故事板模式生效）
```

完整指令列表用 `~help` 查看。

---

## 7. 跨集与多集项目

### 多集复用机制

第 2 集开始，团队会**自动判断**哪些人物/场景是新的、哪些是复用的。
- **新增** → 服化道会重新设计提示词
- **复用** → 直接引用已有的，不重复设计
- **变体**（如冬装版） → 在原版基础上加变化说明

### 全复用集（罕见但要知道）

如果某一集所有人物/场景/音乐都复用前集，团队会写一个空标记块（如 `## ep05（本集全部复用 ep04，无新增）`），保证路由顺畅。

### 修改某一集

```
想重做 ep02 的服化道：
   删除 assets/visual/character-prompts.md 里的 ## ep02 区块
   输入 ~design ep02
```

---

## 8. 故障排除

| 现象 | 原因 | 解决 |
|---|---|---|
| 编剧采访问太多次还没结束 | 你的回答信息密度低 | 一次回答多个问题，或换 full_control 模式逐步推进 |
| 导演审核反复不通过 | 上游产出确实有问题 | 看 FAIL 反馈针对性修改，或手动 ~start 重新触发 |
| Suno 生成的歌不像唐恬风 | Suno 模型理解 Style 描述有限 | 用更具体的乐器/BPM/年代词，而非"唐恬风"三个字 |
| Seedance 视频里人物不对 | 参考图没传或编号错 | 检查 @图片N 的编号顺序，重新上传对应参考图 |
| 进度卡在某个阶段 | 缺少分集标签 | 检查 assets/ 下是否有该集的 `## epXX` 标签 |
| 想完全重来 | 项目跑歪了 | 删除 script/、outputs/、assets/ 内容（保留 .gitkeep） |
| 故事板大图模糊看不清单个 Cut（v7.0） | M4 分镜网格被压缩 | 检查提示词【画幅与排版】段是否明确写「16:9 横版，分镜格大且清晰」；如还不行，砍 Cut 数（从 8 镜降到 5-6 镜） |
| 故事板风格不像预期（v7.0） | AI 决策树选错或 visual_style 漂移 | 用 `~storyboard-style <名>` 手动锁定（5 候选之一），重新 `~prompt <集数>` |
| Seedance 视频与故事板对应 Cut 不像（v7.0） | 视频提示词没引导 Seedance 复刻故事板的对应格 | 检查 video-prompts.md 中每 Cut 描述列是否写「本 Cut 对应 @P0X-storyboard.png 上 Cut N 这一格的构图...」 |
| 故事板大图被平台压成竖版（v7.0） | Image2 / Nano Banana / 即梦默认画幅设置 | 上传前在工具里手动选 16:9 横版；提示词第一段也声明「整体画幅 16:9 横版」 |
| Image2 生出的图人物一致性不如 Nano Banana（v7.0.2） | Image2 多图参考调试还未调优 | 同一提示词拿到 Nano Banana Pro 重生，人物能力这一点上 Nano Banana Pro 依然有优势 |
| 双轨模式锁定后没生效（v7.0） | `.project-config.json` 没写入或分镜师没读 | 检查 `.project-config.json.storyboard_overrides`；如缺失重新 `~storyboard-mode <模式> <集数>` |
| 🔴 **项目是 9:16 / 21:9 / 1:1 但 Seedance 输出 16:9 视频**（v7.0.2 防传染） | Seedance 看到 16:9 横版的 P0X-storyboard.png 参考图就默认输出 16:9 | **三层防御都要做**：① 在 Seedance 工具里**手动选项目 aspect_ratio**；② video-prompts.md 上方有「画幅锁定声明」段；③ 每条 Cut 的「描述」列**开头**有 `[<画幅>构图]` 指令——三个全做才能确保不被故事板传染画幅 |
| 视频提示词复粘到 Seedance 不知道选什么画幅（v7.0.2） | 用户没看 episode-master.md §一字段 | 看 episode-master.md「视觉画幅」字段值（如 9:16），Seedance 里就选 9:16，**不是看故事板大图的 16:9** |

---

## 9. 归档与备份

完成的项目建议：
1. 整个项目目录复制到 `archive/<项目名>/` 备份
2. 把 `outputs/` 下的成品提示词文件单独打包给客户/合作方
3. 优秀的人物/场景提示词可作为新项目的 `input/参考/` 复用

**不要**：
- 删除 `.claude/`（这是团队配置）
- 删除 `CLAUDE.md`（这是主控制文件）
- 修改 SKILL.md（除非你懂在做什么）

---

## 10. 进阶技巧

### 让团队产出更"高级"
- 在 `input/参考/` 里多放高品质参考素材（豆瓣高分电影截图、风格图、参考歌曲）
- 编剧采访时**多说"不要什么"**（拒绝套路化的具体描述比"要什么"更有效）
- 关键场景用 full_control 模式逐子步骤把关

### 引导编剧方向
- 选题文件里写明"参考作品"和"风格基调"
- 采访时直接说出参考的导演/作家/编剧名字（编剧会消化风格不直接抄）

### 修改单个 agent 的产出
- 编剧产出：直接编辑 `script/` 下的版本文件，输入 `~定稿` 晋升
- 导演产出：删除 `outputs/<集数>/01-director-analysis.md`，输入 `~start <集数>` 重新生成
- 服化道：删除 `assets/visual/` 下对应集数标签块，输入 `~design <集数>`
- 音乐：删除 `assets/music/` 下对应集数标签块，输入 `~music <集数>`
- 分镜（v7.0+）：删除 `outputs/<集数>/video-prompts.md` + `episode-master.md` + `storyboards/` 目录，输入 `~prompt <集数>`
- 分镜（v6.1 项目）：删除 `outputs/<集数>/02-seedance-prompts.md`，输入 `~prompt <集数>`（会重生为 v7.0 双轨产物）
- **仅重做某个 P0X 的故事板**（v7.0 新增）：删除 `outputs/<集数>/storyboards/P0X-storyboard-prompt.md` 单文件，然后用 `~feedback <集数> 仅重生 P0X 的故事板提示词，保留其他 P0X 不变` 走反向反馈机制；制片人会调用分镜师只重生该 P0X，其他文件不动

### 想要质量综合报告
分镜完成后会自动生成 `outputs/<集数>/00-quality-report.md`，包含全片各维度评分和改进建议。

---

## 11. 哲学

这个团队的核心信条：

- **少即是多**：能用静默说的不用音乐说，能用一个镜头说的不用三个
- **细节即灵魂**：90% 的高级感来自被认真对待的细节
- **反流水线**：拒绝套路化处理，每一个镜头都要有存在的理由
- **情绪优先**：技术服务于情感，不为炫技而炫技
- **整体感**：从第一帧到最后一帧，每一秒都属于同一个作品的呼吸

如果某次产出让你觉得"还行"但不"惊艳"——回头看是不是这五条没做到。

---

📁 **快速跳转**：

- 主控制文件 → [`CLAUDE.md`](./CLAUDE.md)
- 输入文件夹说明 → [`input/README.md`](./input/README.md)
- Agent 配置 → [`.claude/agents/`](./.claude/agents/)
- 技能配置 → [`.claude/skills/`](./.claude/skills/)
